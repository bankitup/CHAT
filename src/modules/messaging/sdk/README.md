# Messaging SDK

This folder will expose the reusable integration surface for the messaging core.

Planned responsibilities:

- Public entry points used by the web shell.
- Interfaces that future embedded clients can adopt.
- Composition of contract and data layers behind a stable API boundary.

Keep this layer product-agnostic where possible.
