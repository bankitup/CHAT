# Messaging Contract

This folder will hold the durable messaging domain contracts shared across application surfaces.

Planned responsibilities:

- Core messaging types and entity boundaries.
- Conversation and message contract definitions.
- Interfaces that should remain stable across web, embedded, and native clients.
- Future DM E2EE transport contracts that keep plaintext and private keys out of server boundaries.

Keep this layer free of web-specific UI concerns.
