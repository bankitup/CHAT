# Messaging UI

This folder will hold reusable messaging-oriented UI building blocks.

Planned responsibilities:

- Reusable conversation and message presentation primitives.
- UI elements that belong to messaging itself rather than a single page.
- Components that can support the web shell without tightly coupling the core to one route structure.

Keep app-shell concerns outside this folder unless they are clearly messaging-specific.
