/**
 * Compatibility re-export only.
 *
 * Product-specific KeepCozy contract drafts now live in
 * `/src/modules/keepcozy/contract-types.ts`.
 *
 * Shared platform space runtime types live in:
 *
 * - `/src/modules/spaces/model.ts`
 * - `/src/modules/spaces/governance.ts`
 * - `/src/modules/spaces/posture.ts`
 */
export * from '@/modules/keepcozy/contract-types';
